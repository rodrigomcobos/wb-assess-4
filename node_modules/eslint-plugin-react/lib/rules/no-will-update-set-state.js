/**
 * @fileoverview Prevent usage of setState in componentWillUpdate
 * @author Yannick Croissant
 */

'use strict';

const makeNoMethodSetStateRule = require('eslint-plugin-react/lib/util/makeNoMethodSetStateRule');
const testReactVersion = require('eslint-plugin-react/lib/util/version').testReactVersion;

/** @type {import('eslint').Rule.RuleModule} */
module.exports = makeNoMethodSetStateRule(
  'componentWillUpdate',
  (context) => testReactVersion(context, '>= 16.3.0')
);
