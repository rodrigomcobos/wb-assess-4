'use strict';

const getMessageData = require('eslint-plugin-react/lib/util/message');

module.exports = function report(context, message, messageId, data) {
  context.report(
    Object.assign(
      getMessageData(messageId, message),
      data
    )
  );
};
